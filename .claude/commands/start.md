Start both dev servers for this project by running the following two commands in the background simultaneously:

1. In the root directory `c:/Users/georg/Downloads/PTYXIAKH`, run: `npm run dev`
2. In the client directory `c:/Users/georg/Downloads/PTYXIAKH/client`, run: `npm run dev`

After a few seconds, check the output of both and report:
- Whether the backend (nodemon/Express) started successfully or has errors
- The URL where the Vite client is running (usually http://localhost:5173)
